{
  "name": "calculadora",
  "version": "1.0.0",
  "description": "A calculator App to test my skills",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "warning": "electron --trace-warnings .",
    "build": "electron-packager . --platform=win32 --arch=x64 --icon=assets/icons/win/icon.ico --out=release-builds --overwrite --asar --prune=true --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Calculadora\""
  },
  "author": "Pedro Raposo",
  "license": "ISC",
  "devDependencies": {
    "electron": "^23.1.1"
  },
  "dependencies": {
    "electron-drag": "^2.0.0"
  }
}
