{
  "name": "calculadora",
  "version": "1.0.0",
  "description": "A calculator App to test my skills",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "warning": "electron --trace-warnings .",
    "publish": "electron-packager . --overwrite --platform=win32 --arch=ia32 --icon=assets/icons/win/icon.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Calculadora\""
  },
  "author": "Pedro Raposo",
  "license": "ISC",
  "devDependencies": {
    "electron": "^23.1.1"
  },
  "dependencies": {
    "electron-drag": "^2.0.0"
  }
}
